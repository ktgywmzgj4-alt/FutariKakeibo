# AGENTS.md — この仕事の地図

AIがこのリポジトリで作業を始めるとき、**最初に読むファイル**です。
ここには結論だけを書きます。詳しい話は、それぞれの資料へ行ってください。

## 30秒でわかる現状

カップル2人用の家計簿アプリ「ふたり家計簿」を作っています。
iPhone向け。すでに動いていて、TestFlightで実機に配信済みです。

| 項目 | 状態 |
| --- | --- |
| アプリ | iOS 17以降 / SwiftUI / Swift 5 |
| 配信 | TestFlightで内部テスト中 |
| テスト | 62件（CIで全件実行） |
| 共有機能 | コードは完成、CloudKitの設定待ちで**まだ動かない** |

## いちばん大事な制約

**開発機にMacがありません。Windowsだけです。**

つまり **手元でSwiftをコンパイルできません**。
「たぶん動く」と思っても、それは推測です。

- 唯一のコンパイラは **GitHub Actions**（`.github/workflows/ios.yml`）
- 手元でできるのは `bash scripts/check_all.sh` まで（明らかな壊れを見つけるだけ）
- **「実装しました」だけでは完了ではありません。** CIの緑を証拠にしてください

この制約が、この仕事のやり方をほぼ決めています。

## 最初に読む資料

順番に読んでください。全部で10分ほどです。

1. `Docs/mission.md` — 何を、誰のために作るのか
2. `Docs/requirements.md` — 何ができたら完成か
3. `Docs/safety.md` — やってはいけないこと（**必読**）
4. `progress.md` — 前回どこまで進んだか
5. `tasks.json` — 次に何をするか

書く文章の調子を決めるときは `Docs/voice.md`。
検品のやり方は `evals/acceptance.md`。

人間にしかできない作業（CloudKitの設定、実機での確認、Appleへのログイン）は `tasks.json` で
`owner` が「人間」になっています。**AIが代わりにやろうとしないでください。**

## 正しい情報源（迷ったらこれを見る）

推測で答えず、必ず次を確認してください。

| 知りたいこと | 見る場所 |
| --- | --- |
| アプリの仕様 | `Docs/requirements.md` |
| 実際の挙動 | ソースコード（`FutariKakeibo/`）。ドキュメントより**コードが正** |
| CloudKitの項目名 | `FutariKakeibo/Services/CloudKitSyncService.swift` |
| ビルドの通し方 | `.github/workflows/ios.yml` |
| 実機配信の手順 | `Docs/TESTFLIGHT_ON_WINDOWS.md` と `.claude/skills/release/SKILL.md` |
| CloudKitの設定手順（人間の作業） | `Docs/cloudkit-setup.md` |
| 過去の判断とその理由 | `git log`（コミットメッセージに理由を書いています） |
| いま何が終わっているか | `progress.md` と `tasks.json` |

**ドキュメントとコードが食い違ったら、コードが正しい。** ドキュメントを直してください。

## 役割

3つだけです。詳しくは `Docs/roles.md`。

- **計画役** — 何をやるか決める。コードは書かない
- **実行役** — コードを書く。完成条件は変えない
- **検品役** — 証拠を見て合否を出す。自分で直さない

1人のAIが順番に3役を兼ねても構いません。ただし**役を切り替えたと宣言**してから移ってください。

## 作業の型

```
1. progress.md と tasks.json を読む
2. status が "todo" で dependencies が済んでいるタスクを1つ選ぶ
3. status を "doing" にする
4. 作業する
5. bash scripts/check_all.sh を通す
6. ブランチを切ってPRを出す → CIが緑になるのを待つ
7. evidence にCIのURLとテスト件数を書く
8. status を "done" にする
9. progress.md に3行だけ追記する
```

`main` に直接コミットしないでください。必ずブランチとPRを通します。

## セッションが切れたら

会話が切れても、別のAIに変わっても、次の3つを読めば再開できます。

1. `progress.md` の一番下（前回の到達点と、次にやること）
2. `tasks.json` の `status` が `"doing"` のタスク
3. `git log --oneline -10` と `git status`

**`"doing"` のまま放置されたタスクを見つけたら、まずそれを片付けるか `"todo"` に戻してください。**
中途半端な状態を残さないことが、引き継ぎの唯一のコツです。

作業を終えるとき（または長くなりそうなとき）は、必ず `progress.md` を更新してから止まってください。

## この仕事で踏んだ罠

同じ穴に落ちないための記録です。触る前に読んでください。

| 罠 | 何が起きたか |
| --- | --- |
| `project.yml` の `info:` / `entitlements:` | ファイルを「生成」する指定。書いたInfo.plistが毎回上書きされ、カメラの説明文が消えて実機で落ちていた。いまは `INFOPLIST_FILE` で参照している。**戻さないこと** |
| `CKContainer.default()` を起動時に呼ぶ | 署名の無いビルドで例外を投げてアプリごと落ちる。テストが1件も走らなかった。いまは使う直前まで作らない |
| 文字列全体の全角→半角変換 | 数字を直すつもりでカタカナまで半角になった（`ラーメン`→`ﾗｰﾒﾝ`）。数字の範囲だけ変換すること |
| `"\s"` のような正規表現 | Swiftの通常文字列では無効なエスケープ。`#"\s"#` と書く。`scripts/check_swift_escapes.pl` が見つける |
| ビルド番号の固定値 | 2回目のアップロードが重複で弾かれる。いまはビルド時刻から作っている |
| `.p12` をOpenSSL 3の既定で作る | macOSのキーチェーンが読めず「パスワードが違う」と出る。`-legacy` を付ける |
| perl や sed での置換で `\` が消える | `s/\r\n/\n/g` と書いたつもりが、改行そのものに化けた。**この罠を記録しようとして、また踏んだ。** 長い置換はヒアドキュメントに書いてから行番号で差し込む（`CLAUDE.md`） |
| gitを通したファイルはCRLFになる | `perl -0pi` の `\n` が当たらず、置換が静かに失敗する。`\r?\n` で探し、`\r\n` を書く |
| コードをpushした直後にもう一度push | 同じブランチの前のCIが打ち切られ、結果が取れない。**終わるまで待つ** |
| レシート解析の調整 | 直すと別の箇所が壊れる。**実物のレシートからテストを起こしてから**触ること |
